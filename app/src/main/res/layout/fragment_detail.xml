<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="#FBFBFB"
    android:layout_height="match_parent"
    tools:context=".ui.fragment.detailfragment.DetailFragment">

    <include
        android:id="@+id/toolBarTempleDetails"
        layout="@layout/tool_bar"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:id="@+id/relativeLayout"
        app:layout_constraintTop_toBottomOf="@id/toolBarTempleDetails"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/ivTempleDetail"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:scaleType="centerCrop"
            app:layout_constraintDimensionRatio="5:3"
            tools:layout_editor_absoluteX="0dp"
            tools:layout_editor_absoluteY="55dp"
            tools:src="@tools:sample/backgrounds/scenic" />
    </RelativeLayout>

     <RelativeLayout
         app:layout_constraintTop_toBottomOf="@id/relativeLayout"
         app:layout_constraintBottom_toBottomOf="parent"
         android:layout_width="match_parent"
         android:layout_height="0dp">

         <ScrollView
             android:layout_width="match_parent"
             android:layout_height="wrap_content"
             app:layout_constraintBottom_toBottomOf="parent">

             <include
                 android:id="@+id/includeTempleLayout"
                 layout="@layout/temple_details_layout">
             </include>

         </ScrollView>

     </RelativeLayout>


</androidx.constraintlayout.widget.ConstraintLayout>